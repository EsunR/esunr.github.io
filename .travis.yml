sudo: required
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: npm
branches:
  only:
    - source # build master branch only
script:
  - hexo clean
  - hexo generate # generate static files
after_success:
  - hexo deploy